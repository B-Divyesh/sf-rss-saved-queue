const endpoint = document.querySelector('#endpoint'); const deviceKey = document.querySelector('#deviceKey'); const status = document.querySelector('#status'); chrome.storage.local.get({ endpoint: 'https://rss-saved-queue.sociobot.in', deviceKey: '' }).then((values) => { endpoint.value = values.endpoint; deviceKey.value = values.deviceKey; }); document.querySelector('#save').addEventListener('click', async () => { await chrome.storage.local.set({ endpoint: endpoint.value.trim().replace(/\/$/, ''), deviceKey: deviceKey.value.trim() }); status.textContent = 'Settings saved locally.'; });
